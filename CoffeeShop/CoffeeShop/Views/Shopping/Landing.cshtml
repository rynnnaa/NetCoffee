@{
    Layout = "_Layout";
}

we are home

@await Component.InvokeAsync("Individual Product", Model);


<h3>@Model</h3>
<ul>
    @foreach (var todo in Model)
    {
        <li>@Model.Name</li>
        <li>@Model.Description</li>
    }
</ul>